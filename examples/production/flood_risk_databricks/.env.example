# Environment + secrets consumed by the flood-risk pipeline.
#
# In CI, UC identifiers are passed to ``databricks bundle deploy`` via
# ``--var`` sourced from GitHub environment secrets. API credentials are
# stored in a Databricks secret scope (default ``flood-risk``) and read
# at runtime by the notebook wrapper.
#
# Template only - do NOT commit real values.

# Unity Catalog identifiers.
TELM_CATALOG=<unity-catalog-name>
TELM_SCHEMA=<unity-schema-name>
ADDRESS_SOURCE_TABLE=<source-table-with-id-address-columns>

# Databricks auth (service principal + OAuth).
DATABRICKS_HOST=https://<your-workspace>.cloud.databricks.com
DATABRICKS_CLIENT_ID=<service-principal-application-id>
DATABRICKS_CLIENT_SECRET=<oauth-secret>

# API credentials - stored in the Databricks secret scope, NOT in CI:
#   databricks secrets create-scope flood-risk
#   databricks secrets put-secret flood-risk tomtom_api_key
#   databricks secrets put-secret flood-risk jba_basic_auth
# (jba_basic_auth should already include the 'Basic ' prefix.)
