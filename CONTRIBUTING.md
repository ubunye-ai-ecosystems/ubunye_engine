# Contributing

- Use **type hints**, **docstrings**, and add **unit tests** for new code.
- Format with **black**, lint with **ruff** (configured via pyproject).
- Keep PRs small, with clear rationale and migration notes.
